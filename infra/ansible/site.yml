---
- name: Demo playbook
  hosts: local
  gather_facts: false
  tasks:
    - name: Ensure demo file exists
      ansible.builtin.copy:
        dest: /tmp/devsecops_demo.txt
        content: "Hello from Ansible!"
        mode: "0644"

